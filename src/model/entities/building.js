/**
 * @overview Building entitiy
 *
 * This is likely to change
 */

let Meter = require('./Meter');

let Building = {
    tableName: 'buildings',
    idAttribute: 'id'
};

module.exports = Building;
